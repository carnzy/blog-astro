# 代码变更日志 - 删除音乐模块

> 变更时间：2026-05-31
> 变更类型：功能移除
> 影响范围：音乐播放器模块全部移除

---

## 删除的文件

| # | 文件路径 | 类型 |
|---|----------|------|
| 1 | `src/components/widget/Music.astro` | 组件 |
| 2 | `src/components/features/MusicPlayer.astro` | 组件 |
| 3 | `src/components/features/MusicManager.astro` | 组件 |
| 4 | `src/config/musicConfig.ts` | 配置 |
| 5 | `public/assets/music/` (整个目录) | 资源 |

## 修改详情

### 1. `src/config/index.ts`
**变更**：删除 2 行
- 删除 `MusicPlayerConfig,` 类型导出
- 删除 `export { musicPlayerConfig } from "./musicConfig";` 配置导出

### 2. `src/types/config.ts`
**变更**：删除 49 行
- 从 `WidgetComponentType` 中删除 `| "music"`
- 删除整个 `MusicPlayerConfig` 类型定义（含 meting、local 等子类型）

### 3. `src/config/sidebarConfig.ts`
**变更**：删除 16 行
- 从 `leftComponents` 中删除音乐组件配置项（7行）
- 从 `mobileBottomComponents` 中删除音乐组件配置项（6行）

### 4. `src/layouts/Layout.astro`
**变更**：删除 2 行
- 删除 `import MusicManager from "@components/features/MusicManager.astro";`
- 删除 `<MusicManager />`

### 5. `src/components/layout/Navbar.astro`
**变更**：删除/修改 18 行
- 删除 `MusicPlayer` 导入
- 删除 `musicPlayerConfig` 导入
- 删除导航栏音乐按钮模板（5行）
- 删除导航栏音乐弹出面板模板（4行）
- 删除脚本中音乐按钮事件处理（9行）
- 从点击外部关闭数组中移除音乐面板和按钮引用
- 移除未使用的 `I18nKey` 和 `i18n` 导入

### 6. `src/components/layout/SideBar.astro`
**变更**：删除 2 行
- 删除 `import Music from "@/components/widget/Music.astro";`
- 从 `componentMap` 中删除 `music: Music,`

### 7. `src/global.d.ts`
**变更**：删除 43 行
- 删除 `__fireflyMusic` 完整的类型声明（包括 init、getState、所有播放控制方法）

### 8. `src/env.d.ts`
**变更**：删除 1 行
- 删除 `// __fireflyMusic type is defined in global.d.ts` 注释

### 9. `src/i18n/i18nKey.ts`
**变更**：删除 20 行
- 删除 `music = "music"` 枚举值
- 删除 19 个音乐相关枚举值（musicNoPlaying ~ musicAudioPlayer）

### 10. 语言文件 (5个文件)
**变更**：每个文件删除 19 行，共 95 行

| 文件 | 删除行范围 |
|------|-----------|
| `src/i18n/languages/zh_CN.ts` | 第 24-42 行 |
| `src/i18n/languages/en.ts` | 第 24-42 行 |
| `src/i18n/languages/ja.ts` | 第 24-42 行 |
| `src/i18n/languages/ru.ts` | 第 24-42 行 |
| `src/i18n/languages/zh_TW.ts` | 第 24-42 行 |

### 11. 文档文件 (2个)
- `src/components/README.md`：删除 MusicManager 和 MusicPlayer 组件说明
- `src/config/README.md`：删除 musicConfig.ts 的文件树条目和说明

## 统计

| 指标 | 数值 |
|------|------|
| 删除文件数 | 5 |
| 修改文件数 | 14 |
| 删除总行数 | ~260 |
| 新增行数 | 0 |

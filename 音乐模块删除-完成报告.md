# 音乐模块删除 - 完成报告

## 任务概述

删除网页中 XPATH 为 `//widget-layout[@data-id='music-widget-oickwrg-layout']` 的音乐播放器模块，包括其全部前端组件、配置、类型声明、国际化翻译及相关资源文件。

## 删除的文件 (4个)

| 文件 | 说明 |
|------|------|
| `src/components/widget/Music.astro` | 音乐播放器小部件包装组件 |
| `src/components/features/MusicPlayer.astro` | 音乐播放器 UI 视图组件（含完整播放控制、歌词显示、播放列表） |
| `src/components/features/MusicManager.astro` | 全局音乐播放管理器（单例模式，管理 audio 元素和播放状态，通过 CustomEvent 同步） |
| `src/config/musicConfig.ts` | 音乐播放器配置（Meting API / 本地音乐模式） |

## 删除的资源目录 (1个)

| 路径 | 说明 |
|------|------|
| `public/assets/music/` | 音乐资源文件（含本地 mp3 和封面图片） |

## 修改的文件 (14个)

### 配置层

| 文件 | 变更内容 |
|------|----------|
| `src/config/index.ts` | 移除 `MusicPlayerConfig` 类型导出；移除 `musicPlayerConfig` 配置导出 |
| `src/types/config.ts` | 从 `WidgetComponentType` 联合类型中移除 `"music"`；删除 `MusicPlayerConfig` 类型定义 |
| `src/config/sidebarConfig.ts` | 从 `leftComponents` 中移除音乐组件配置项；从 `mobileBottomComponents` 中移除音乐组件配置项 |
| `src/config/README.md` | 移除 `musicConfig.ts` 的文件树条目和说明 |

### 布局层

| 文件 | 变更内容 |
|------|----------|
| `src/layouts/Layout.astro` | 移除 `MusicManager` 导入和 `<MusicManager />` 组件实例 |
| `src/components/layout/Navbar.astro` | 移除 `MusicPlayer` 导入和 `musicPlayerConfig` 导入；移除导航栏音乐按钮和弹出面板；移除脚本中的音乐按钮事件处理；移除点击外部关闭面板中的音乐面板引用；移除未使用的 `I18nKey` 和 `i18n` 导入 |
| `src/components/layout/SideBar.astro` | 移除 `Music` 组件导入；从 `componentMap` 中移除 `music: Music` 映射 |

### 类型与国际化

| 文件 | 变更内容 |
|------|----------|
| `src/global.d.ts` | 移除 `__fireflyMusic` 全局类型声明（43行） |
| `src/env.d.ts` | 移除 `__fireflyMusic type is defined in global.d.ts` 注释 |
| `src/i18n/i18nKey.ts` | 移除 `music` 键和全部 19 个音乐小部件相关 i18n 键 |
| `src/i18n/languages/zh_CN.ts` | 移除 19 行音乐相关中文翻译 |
| `src/i18n/languages/en.ts` | 移除 19 行音乐相关英文翻译 |
| `src/i18n/languages/ja.ts` | 移除 19 行音乐相关日文翻译 |
| `src/i18n/languages/ru.ts` | 移除 19 行音乐相关俄文翻译 |
| `src/i18n/languages/zh_TW.ts` | 移除 19 行音乐相关繁中翻译 |

### 文档

| 文件 | 变更内容 |
|------|----------|
| `src/components/README.md` | 移除 `MusicManager` 和 `MusicPlayer` 组件说明 |

## 验证结果

- ✅ `astro check` 通过：0 errors, 0 warnings, 0 hints
- ✅ 全项目搜索无残留引用（`music-widget`, `MusicPlayer`, `MusicManager`, `musicPlayerConfig`, `__fireflyMusic`）
- ✅ Bangumi 番组计划的音乐分类功能（`bangumiCategoryMusic`）不受影响

## 影响范围

- 侧边栏音乐播放器小部件：已移除
- 导航栏音乐播放器入口：已移除
- 移动端底部音乐组件：已移除
- 全局音乐管理器单例：已移除
- 本地音乐资源文件：已移除
